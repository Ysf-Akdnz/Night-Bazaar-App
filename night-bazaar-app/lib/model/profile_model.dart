import 'package:flutter/material.dart';

class AccountModel extends StatelessWidget {
  const AccountModel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        
    );
  }
}